<?xml version="1.0" encoding="UTF-8"?>
<project name="LowbrainMobs" default="compileLowbrainMobs" basedir=".">
    <target name="compileLowbrainMobs" description="Create a jar for the project">
        <mkdir dir="target"></mkdir>
        <jar destfile="target/${ant.project.name}.jar">
            <fileset dir="bin"></fileset>
            <fileset dir="resources"></fileset>
        </jar>
    </target>
</project>
